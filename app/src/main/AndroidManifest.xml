<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.satyam.newswala">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".newscategory.CategoryNews"></activity>
        <activity android:name=".newscategory.CategoryNewsEco"></activity>
        <activity android:name=".newscategory.CategoryNewsTech"></activity>
        <activity android:name=".newscategory.CategoryNewsPol"></activity>
        <activity android:name=".newscategory.CategoryNewsEnter"></activity>
        <activity android:name=".newscategory.CategoryNewsSci"></activity>
        <activity android:name=".fullArticles.FullArticle" />
        <activity android:name=".splash.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".main.MainActivity"
            android:windowSoftInputMode="adjustPan" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>